import React from 'react'

function NavBar() {
    // if (width < BREAKPOINT) {
    //     return <MobileNavbar BREAKPOINT={BREAKPOINT} />;
    //   }
    //   return <DesktopNavbar />;
  return (
    <div>NavBar</div>
  )
}

export default NavBar